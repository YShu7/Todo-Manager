<div id="form_head"><%= f.label :name, "Your group" %></div><br>
<%= f.text_area :name, class: "form-control" %><br>

<% if @group.errors.any? %>
	<ul class="alert alert-danger">
	<% @group.errors.full_messages.each do |msg| %>
		<li> <%= msg %> </li>
	<% end %>
	</ul>
<% end %>