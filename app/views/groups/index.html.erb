<h1>Todo Manager</h1>

<ul>
<% @groups.each do |group| %>
	<li> 
	<%= link_to group.name, group_tasks_path(group) %> 
	</li>
	<br>
<% end %>
</ul>

<br><%= link_to 'View All', overview_groups_path %>
<br><%= link_to 'Create', new_group_task_path(:group_id => 4) %>