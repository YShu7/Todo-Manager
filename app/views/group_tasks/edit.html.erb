<%= form_for ([@group, @task]) do |f| %> 

	<%= render :partial => 'form', :locals => {:f => f} %><br>
	<br>
	<%= f.submit "Update", class: " btn btn-warning btn-block" %>

<% end %> 

<br><%= link_to 'Homepage', group_tasks_path(@group), class: " btn btn-primary btn-block" %>